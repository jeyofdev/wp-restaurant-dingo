{{ args.before_widget|raw }}

    {{ args.before_title|raw }}
        {{ instance.title }}
    {{ args.after_title|raw }}

    <div class="contact_info">
        <p><span>{{ __("Address : ", "dingo") }}</span>{{ contact.address }}</p>
        <p><span>{{ __("Phone : ", "dingo") }}</span>{{ contact.phone }}</p>
        <p><span>{{ __("Email : ", "dingo") }}</span>{{ contact.email }}</p>
    </div>

{{ args.after_widget|raw }}